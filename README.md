# Iclirius Homebrew Tap

Install the Iclirius release candidate with:

```bash
brew tap iclirius/tap
brew install iclirius/tap/iclirius
```

This tap contains distribution metadata only. Source code remains in the
private `iclirius/iclirius-openclaw` repository. Release artifacts are published
from `iclirius/iclirius-distribution`.
